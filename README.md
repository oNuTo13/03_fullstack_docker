# Full-Stack Web App + Docker + Jenkins CI/CD

เว็บแอปนี้เป็นโปรเจกต์ Full-Stack ที่ประกอบด้วย:

- **Backend API** (Node.js + Express + MySQL)
- **Frontend** (React หรือ HTML/JS ตามที่คุณใช้)
- **Database (MySQL)** พร้อมตารางและ seed data
- **Docker + Docker Compose** สำหรับรันทั้งระบบ
- **Jenkins CI/CD Pipeline** สำหรับ build → deploy → health check อัตโนมัติ

โปรเจกต์นี้ทำตามโจทย์:  
“พัฒนาเว็บแอป Full-Stack + Database รันผ่าน Docker และตั้งค่า Jenkins ให้ทำ CI/CD อัตโนมัติ”

---

## 📌 ฟีเจอร์ของระบบ

### ✔ Backend (API)
- Endpoint `/health` → ตรวจสอบว่า API ทำงาน + database เชื่อมต่อได้
- Endpoint `/places` หรือ `/items` (แล้วแต่ที่คุณใช้) → ดึงข้อมูลจาก MySQL

### ✔ Frontend
- ดึงข้อมูลจาก API ผ่าน fetch/axios
- แสดงผลข้อมูลแบบเป็น list หรือ card

### ✔ Database
- MySQL พร้อมตาราง เช่น `places`
- มี seed data 1–3 record

### ✔ CI/CD (Jenkins)
- Pull code จาก GitHub
- Build Docker image
- Deploy ด้วย `docker compose up -d`
- Health check อัตโนมัติ

---

## 📁 โครงสร้างไฟล์

